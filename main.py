from screen import start_capture
from controller import MinecraftController
from ultralytics import YOLO

# 1. Запускаем твои «глаза» (как у тебя и было)

# 2. Инициализируем «руки» (добавили скобки (), чтобы создать объект)
# controller = MinecraftController()

# 3. Загружаем «мозг» напрямую через YOLO, используя твой путь к весам
# model = YOLO("D:/Python project/first ai/Minecraft AI agent/runs/detect/train-6/weights/best.pt")

start_capture()
